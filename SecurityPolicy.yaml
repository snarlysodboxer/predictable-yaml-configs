---
apiVersion: gateway.envoyproxy.io/v1alpha1  # first, required
kind: SecurityPolicy  # required
metadata:  # required
  name: TODO  # first, required
  namespace: TODO  # required
  labels:  # required
    app: TODO  # first
    app.kubernetes.io/name: TODO  # required
  annotations: {}
spec:  # preferred
  targetRefs:  # first, required
  - group: TODO  # first, preferred
    kind: TODO  # preferred
    name: TODO  # required
  oidc:
    provider:
      issuer: TODO
    clientID: TODO
    clientSecret:
      name: TODO  # required
    redirectURL: TODO
    cookieDomain: TODO
    scopes: []

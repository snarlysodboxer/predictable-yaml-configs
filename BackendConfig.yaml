---
apiVersion: cloud.google.com/v1  # first, required
kind: BackendConfig  # required
metadata:  # required
  name: TODO  # first, required
  namespace: TODO  # preferred
  labels:  # required
    app: TODO  # first
    app.kubernetes.io/name: TODO  # required
  annotations: {}
spec:  # required
  iap:
    enabled: TODO
    oauthclientCredentials:
      secretName: TODO

---
apiVersion: apiregistration.k8s.io/v1  # first, required
kind: APIService  # required
metadata:  # required
  name: TODO  # first, required
  labels:  # required
    app: TODO  # first
    app.kubernetes.io/name: TODO  # required
  annotations: {}
spec:  # required
  group: TODO
  groupPriorityMinimum: TODO
  insecureSkipTLSVerify: TODO
  service:
    name: TODO  # first, required
    namespace: TODO  # preferred
  version: TODO
  versionPriority: TODO

---
apiVersion: core/v1  # first, required
kind: Pod  # required
metadata:  # required
  name: TODO  # first, required
  namespace: TODO  # preferred
  labels:  # required
    app: TODO  # first
    app.kubernetes.io/name: TODO  # required
  annotations: {}
spec:  # required
  serviceAccountName: TODO  # first, preferred
  automountServiceAccountToken: TODO  # preferred
  initContainers: {}  # ditto=.spec.containers
  restartPolicy: TODO
  containers:  # required
  - name: TODO  # first, required
    image: TODO  # required
    imagePullPolicy: TODO  # required
    command:  # preferred
    - TODO
    args:  # preferred
    - TODO
    envFrom:
    - secretRef:
        name: TODO  # first
    - configMapRef:
        name: TODO  # first
    env:
    - name: TODO  # first, required
    ports:
    - name: TODO  # first, required
      containerPort: TODO  # preferred
      hostPort: TODO
      protocol: TODO
    resources:  # preferred
      requests:  # preferred
        cpu: TODO  # preferred
        memory: TODO  # preferred
      limits:  # preferred
        cpu: TODO  # preferred
        memory: TODO  # preferred
    lifecycle: {}
    startupProbe:  # preferred
      periodSeconds: TODO  # preferred
      initialDelaySeconds: TODO  # preferred
      timeoutSeconds: TODO  # preferred
      successThreshold: TODO  # preferred
      failureThreshold: TODO  # preferred
      httpGet:  # preferred
        port: TODO  # first, required
        path: TODO  # required
        scheme: TODO
    livenessProbe: {}  # ditto=.spec.containers[0].startupProbe
    readinessProbe: {}  # ditto=.spec.containers[0].startupProbe
    volumeMounts:
    - name: TODO  # first, required
      mountPath: TODO  # preferred
      subPath: TODO
      readOnly: TODO
    securityContext:
      privileged: TODO
      allowPrivilegeEscalation: TODO
      capabilities:
        drop: []
  terminationGracePeriodSeconds: TODO
  dnsPolicy: TODO
  securityContext:
    runAsNonRoot: TODO
    runAsUser: TODO
    runAsGroup: TODO
    seccompProfile:
      type: TODO
  volumes:
  - name: TODO  # first, required
    configMap:
      name: TODO  # first, required
      defaultMode: TODO
      optional: TODO
      items:
      - key: TODO  # first
        path: TODO
    secret:
      secretName: TODO  # required
      defaultMode: TODO
      optional: TODO
      items:
      - key: TODO  # first
        path: TODO
  nodeSelector: {}
  tolerations:
  - effect: TODO  # first, preferred
    key: TODO  # preferred
  affinity:
    podAntiAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
      - topologyKey: TODO
        labelSelector:
          matchLabels:
            app: TODO  # first
            app.kubernetes.io/name: TODO
          matchExpressions:
          - key: TODO  # first, required
            operator: TODO  # required
            values: []  # required
        nodeSelectorTerms:
        - matchLabels:
            app: TODO  # first
            app.kubernetes.io/name: TODO
          matchExpressions:
          - key: TODO  # first, required
            operator: TODO  # required
            values: []  # required
      preferredDuringSchedulingIgnoredDuringExecution:
      - weight: TODO  # preferred
        podAffinityTerm:  # preferred
          topologyKey: TODO  # preferred
          labelSelector:  # preferred
            matchLabels:  # preferred
              app: TODO  # first, preferred
              app.kubernetes.io/name: TODO  # preferred
            matchExpressions:
            - key: TODO  # first, required
              operator: TODO  # required
              values: []  # required

---
apiVersion: v1  # first, required
kind: PersistentVolume  # required
metadata:  # required
  name: TODO  # first, required
  labels:  # required
    app: TODO  # first
    app.kubernetes.io/name: TODO  # required
  annotations: {}
spec:  # required
  storageClassName: TODO  # preferred
  capacity:
    storage: TODO
  persistentVolumeReclaimPolicy: TODO
  accessModes: []
  mountOptions: []
  csi:
    driver: TODO
    volumeHandle: TODO
    volumeAttributes: {}

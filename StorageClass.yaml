---
apiVersion: storage.k8s.io/v1  # first, required
kind: StorageClass  # required
metadata:  # required
  name: TODO  # first, required
  labels:  # required
    app: TODO  # first
    app.kubernetes.io/name: TODO  # required
  annotations: {}
provisioner: TODO
reclaimPolicy: TODO
allowVolumeExpansion: TODO
parameters: {}

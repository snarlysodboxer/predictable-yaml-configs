---
apiVersion: v1  # first, required
kind: Service  # required
metadata:  # required
  name: TODO  # first, required
  namespace: TODO  # preferred
  labels:  # required
    app: TODO  # first
    app.kubernetes.io/name: TODO  # required
  annotations: {}
spec:  # required
  type: TODO  # first, required
  clusterIP: TODO
  sessionAffinity: TODO
  loadBalancerIP: TODO
  selector:  # preferred
    app: TODO  # first
    app.kubernetes.io/name: TODO  # required
  ports:
  - name: TODO  # first, required
    port: TODO  # required
    targetPort: TODO  # preferred
    protocol: TODO

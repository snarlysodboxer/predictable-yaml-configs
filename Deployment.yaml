---
apiVersion: apps/v1  # first, required
kind: Deployment  # required
metadata:  # required
  name: TODO  # first, required
  namespace: TODO  # preferred
  labels:  # required
    app: TODO  # first
    app.kubernetes.io/name: TODO  # required
  annotations: {}
spec:  # required
  replicas: TODO  # first
  revisionHistoryLimit: TODO
  minReadySeconds: TODO
  selector:  # required
    matchLabels:  # preferred
      app: TODO  # first
      app.kubernetes.io/name: TODO  # required
  strategy:  # preferred
    type: TODO  # first, required
    rollingUpdate:
      maxSurge: TODO
      minAvailable: TODO
      maxUnavailable: TODO
  template:  # required
    metadata:  # required
      name: TODO
      namespace: TODO
      labels:  # required
        app: TODO  # first
        app.kubernetes.io/name: TODO  # required
      annotations: {}
    spec: {}  # ditto=Pod.spec, required

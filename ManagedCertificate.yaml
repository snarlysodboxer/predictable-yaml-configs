---
apiVersion: networking.gke.io/v1  # first, required
kind: ManagedCertificate  # required
metadata:  # required
  name: TODO  # first, required
  namespace: TODO  # preferred
  labels:  # required
    app: TODO  # first
    app.kubernetes.io/name: TODO  # required
  annotations: {}
spec:  # required
  domains: []

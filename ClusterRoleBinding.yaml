---
apiVersion: rbac.authorization.k8s.io/v1  # first, required
kind: ClusterRoleBinding  # required
metadata:  # required
  name: TODO  # first, required
  labels:  # required
    app: TODO  # first
    app.kubernetes.io/name: TODO  # required
  annotations: {}
roleRef:  # required
  apiGroup: TODO  # first, required
  kind: TODO  # required
  name: TODO  # required
  namespace: TODO  # preferred
subjects:  # preferred
- kind: TODO  # first, required
  name: TODO  # required
  namespace: TODO  # preferred
